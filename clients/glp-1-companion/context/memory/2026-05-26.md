# 2026-05-26

## Session 1

### Project
app-redesign

### Goal
Figure out what's next for the mobile app; save a corrected ship runbook for Phase 9.

### Key finding
- The mobile app's entire REST API is uncommitted/undeployed. Backend repo
  (`palmquist66/glp1-companion`) is even with origin/main, but `/api/today`, `/api/trends`,
  `/api/log`, `/api/meds`, `/api/user`, `/api/onboarding`, `/api/notifications/device-token`,
  and `/api/sync/health/*` are all untracked, plus schema.prisma + several lib files modified.
- Means the Expo app has only run against a LOCAL backend. Against production today, nothing
  works (not just health sync). Backend deploy must happen BEFORE native build.
- The May 23 runbook missed this entirely.

### Deliverables
- `projects/briefs/app-redesign/2026-05-26_phase9-ship-runbook.md` — corrected 4-stage runbook
  (ship backend → migrate prod DB → build/test mobile → parallel long-lead). Has caveats +
  checklist.
- Marked `2026-05-23_phase9-health-sync-runbook.md` SUPERSEDED.

### Decisions
- Stage order: commit+push backend → migrate prod DB (additive, backward-compatible) → then
  native deps/prebuild/EAS build. Verify deploy with `curl .../api/trends` expecting 401.
- Flagged gotchas: eas.json dev profile is `ios.simulator:true` (HealthKit needs a real
  device → set false); device build needs paid Apple Developer account; mobile/.env missing.

## Session 2

### Goal
Agent-side prep before James runs the Phase 9 ship runbook.

### Done
- FIXED the 3 type errors in `mobile/components/profile/health-targets-card.tsx` — `TargetRow`
  prop was typed `(typeof Colors)["light"]`; changed to the existing exported `AppColors`
  union. Mobile `tsc --noEmit` now clean (0 errors). Backend `tsc --noEmit` also clean.
- Full pre-flight review of the Phase 9 sync code (mobile health.ts + health-sync.ts, backend
  health-import.ts, full auth chain).

### Review findings (folded into the runbook)
- CRITICAL: `DEMO_MODE=true` in prod opens the whole API — `proxy.ts` skips auth and
  `getCurrentUser()` returns the first DB user to anyone. Added a "confirm DEMO_MODE off"
  step to Stage 2 + checklist. James must verify in Vercel env.
- MEDIUM (data-loss): sync cursors (iOS anchors / Android `since`) advance INSIDE pullHealthData,
  before the import POST. Failed POST = permanently lost window. Backend import is idempotent,
  so fix = advance cursors only after POST success. Flagged in runbook caveats; needs own change
  + device test, not bundled into prep.
- MINOR: first-sync history asymmetry (Android 30d vs iOS all-history); device-token route is
  under public matcher but self-gates via getCurrentUser (safe).
- HEALTHY: mobile auth chain fully wired (Clerk getToken → setAuthTokenGetter in _layout.tsx →
  Bearer header → middleware auth.protect → getCurrentUser). All new mobile API routes protected.

### Did NOT do (and why)
- Did NOT generate the Prisma migration — needs a live DB (James's machine). Stays Stage 1.
- Did NOT commit/push the backend — DEMO_MODE is a judgment call against his Vercel env, it's his
  prod repo + the push is the irreversible part, and there's a stray `test-waitlist.ts` to keep
  out. Gave him the exact commit command (with the test-waitlist.ts exclusion) in Stage 1.
- Kept prep lightweight/direct rather than spinning up `/gsd:quick` for a 1-line type fix + a
  packaging commit (mobile/ isn't even version-controlled). Offered GSD if he wants the trail.

### Committed (not pushed)
- `ba8b493` in the app/ repo (palmquist66/glp1-companion): mobile REST API layer + Phase 9
  health sync backend + previously-untracked add_device_token migration. Branch ahead 1 of
  origin/main, NOT pushed. test-waitlist.ts deliberately excluded (still untracked).
- phase9_health_sync migration NOT yet generated (needs James's DB — Stage 1 step 2).

### Correction (caught during wrap-up)
- Git root is `AgneticOS/agentic-os/` (remote: palmquist66/AgneticOS). The Expo `mobile/` app
  IS tracked by this monorepo — I told James mid-session it "wasn't version controlled," which was
  wrong (I'd run git from the parent `AgneticOS/` dir). Only the `app/` backend is a separate
  nested repo (remote palmquist66/glp1-companion, deploys to Vercel). Use
  `git rev-parse --show-toplevel` next time.

### Open threads
- James proceeds from Stage 1 step 2: `prisma migrate dev` (his DB), then `git add` the new
  migration + commit, then push to glp1-companion (after confirming DEMO_MODE off in Vercel).
  I did NOT run the migration — it writes to his DB and I shouldn't guess local vs prod target.
- Backend commit `ba8b493` (glp1-companion repo) is local, NOT pushed.
- Things I can't do: push the deploy (irreversible), package installs (denied by perms, blocks
  expo install/prebuild), eas init/build (interactive login + Apple creds), device test.
- Offer to fix the sync-cursor data-loss bug as a focused follow-up after the first device test.
