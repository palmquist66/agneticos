# 2026-04-28

## Session 1

### Project
app-redesign

### Goal
Finish the remaining design specs (onboarding flow + data sync hub) to complete the brief

### Deliverables
- `projects/briefs/app-redesign/2026-04-28_onboarding-flow.md` — Full onboarding spec: 6-step flow, state management, resume logic, edge cases, per-source connection details, analytics events
- `projects/briefs/app-redesign/2026-04-28_data-sync-hub.md` — Data sync hub spec: architecture, DB schema (DataSourceConnection + SyncLog), per-source connection flows (Dexcom/Google Fit/Apple Health), sync triggers, error handling, security, API endpoints, shared provider interface

### Decisions
- Onboarding and data sync hub written as standalone deep specs (not just the surface coverage in the IA/screen specs docs)
- Both specs reference and extend the existing screen specs (S3-S6, S14) without contradicting them
- All 7 brief deliverables now checked off — design phase is complete

### Open threads
- All design specs are done — next step is Phase 1 of the implementation plan (project setup, auth, database, app shell)
- Dexcom developer access was validated in sandbox on April 21 — ready for build
- Open questions from brief still stand: Streamlit vs Next.js answered (Next.js), Apple Health support (Phase 2 via Capacitor), auto-sync (sync-on-open + Apple Health background delivery), AI chat placement (inline on Trends)

## Session 2

### Project
app-redesign

### Goal
Execute Phase 1 of the implementation plan — Next.js project setup, Clerk auth, Prisma database schema, app shell with bottom nav + FAB

### Deliverables
- `projects/briefs/app-redesign/app/` — Full Next.js 16 project with App Router, TypeScript, Tailwind CSS 4
- `app/prisma/schema.prisma` — 11-table PostgreSQL schema (User, GlucoseLog, WeightLog, FoodLog, MedicationLog, MedicationSchedule, SideEffect, InjectionSite, TitrationSchedule, DataSourceConnection, SyncLog)
- `app/src/proxy.ts` — Clerk auth with route protection (Next.js 16 proxy pattern)
- `app/src/components/nav/` — Bottom nav (4 tabs + FAB), FAB action sheet (7 options), app header
- `app/src/app/(app)/` — 4 main tab pages with contextual empty states, 7 log route placeholders
- `app/src/app/(auth)/` — Clerk sign-in/sign-up pages
- `app/src/app/onboarding/` — 4-step onboarding flow placeholders
- `app/src/app/page.tsx` — Landing page with hero CTA

### Decisions
- Next.js 16 renames middleware.ts to proxy.ts — used proxy.ts for Clerk auth
- shadcn/ui now uses @base-ui/react (no asChild prop) — used buttonVariants() + Link instead
- Prisma v7 constructor changed — uses prisma.config.ts for datasource URL, not constructor arg
- Landing page at root app/page.tsx, (marketing) route group reserved for future pages (/about, /pricing)

### Open threads
- Phase 2 (Logging) is next — all 7 FAB logging flows need real forms and API routes
- Need to set up Clerk keys in .env to test auth flow locally
- Need to provision PostgreSQL database (Neon or Supabase) and run prisma migrate
- The _middleware.ts.bak file should be deleted (leftover from rename)
