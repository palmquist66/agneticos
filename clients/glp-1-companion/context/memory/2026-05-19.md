# 2026-05-19

## Session 1

### Project
app-redesign

### Goal
Implement Phase 6: Push Notifications for the mobile app

### Deliverables
- `app/prisma/schema.prisma` — added DeviceToken model with userId, token, platform, active fields
- `app/prisma/migrations/20260520012659_add_device_token/` — database migration
- `app/src/app/api/notifications/device-token/route.ts` — POST/DELETE endpoints for device token registration
- `app/src/lib/push-server.ts` — refactored to dispatch to both web (VAPID) and Expo push in parallel
- `app/src/app/api/notifications/send/route.ts` — updated query to include device token subscribers
- `mobile/app.json` — added expo-notifications plugin
- `mobile/lib/notifications.ts` — registration, unregistration, iOS categories, response handler
- `mobile/app/_layout.tsx` — notification listeners wired into AuthGate
- `mobile/components/profile/notification-toggle.tsx` — three-state toggle (undetermined/granted/denied)
- `mobile/app/(tabs)/profile.tsx` — added NotificationToggle, ScrollView, sign-out unregistration
- `packages/shared/types.ts` — added RegisterDeviceTokenResponse type

### Decisions
- Separate DeviceToken model from PushSubscription (different data shape — Expo tokens vs W3C push subscriptions)
- iOS notification categories with "Mark as Taken" and "Snooze 15min" action buttons reuse existing action token endpoints
- Notification permission requested from Profile toggle, not on first launch
- Used SDK 54 APIs (shouldShowBanner/shouldShowList instead of shouldShowAlert, subscription.remove() instead of removeNotificationSubscription)

### Open threads
- Phase 7 (Health Targets editing on Profile) is next
- Need physical device testing for push notification flow end-to-end
- EAS project ID needs to be configured in app.json extra.eas.projectId for production push tokens
