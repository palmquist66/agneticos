# 2026-05-06

## Session 1

### Project
app-redesign

### Goal
Fix Mac Mini crash when running the app on port 3010, then continue applying design system colors

### Deliverables
- `src/lib/trends-queries.ts` — Added `take:` limits to all 9 unbounded queries across getWeightTrend, getGlucoseTrend, getPatternData, getAIContextData
- `src/lib/today-queries.ts` — Added `take: 10` to all 5 getRecentActivity queries
- `src/components/trends/ai-chat.tsx` — Added AbortController with cleanup on unmount to prevent stream memory leaks
- `src/components/trends/glucose-trend-chart.tsx` — Wrapped filtered data + computed values in useMemo
- `src/lib/db.ts` — Bumped Prisma connection pool from 5 to 10
- `src/app/(app)/trends/page.tsx` — Replaced `force-dynamic` with `revalidate = 60`
- `src/app/api/export/doctor-report/route.ts` — Added `take:` limits to all 5 unbounded queries
- `src/components/today/activity-item.tsx` — Color-coded icons per entry type (teal/blue/amber/coral/red)
- `src/components/today/medication-checkin.tsx` — Pill icon now coral
- `src/components/log/number-input.tsx` — +/- buttons teal-tinted
- `src/components/trends/deep-analysis.tsx` — Brain icon teal, CTA button solid teal
- `src/components/today/recent-activity.tsx` — Empty state circle now teal
- `src/app/onboarding/ready/page.tsx` — Replaced emoji with colored icon circles

### Decisions
- Root cause of crash: unbounded DB queries + force-dynamic + Turbopack dev mode creating memory spiral
- Running `next build && next start` instead of `next dev` avoids Turbopack overhead and is stable for testing
- Previous crash-fix session was never saved to memory (machine crashed before save) — all fixes applied fresh

### Open threads
- More color refinement possible on profile cards, remaining log forms
- Dark mode not yet visually verified
- Logo SVG mismatch from 05-05 still open

## Session 2

### Project
app-redesign

### Goal
Implement medication reminder push notifications

## Session 3

### Goal
Create Twitter/X profile assets for @GLP_1Companion — logo, banner, and bio copy

### Deliverables
- `projects/viz-nano-banana/2026-05-06_glp1-profile-pic.png` — Abstract spiral logo mark (v1, blue palette — user's preferred shape)
- `projects/viz-nano-banana/2026-05-06_glp1-profile-pic-v2.png` — Hexagon + pulse mark (v2, blue — rejected)
- `projects/viz-nano-banana/2026-05-06_glp1-profile-pic-v3.png` — Spiral mark in app's teal/coral/amber palette (v3, final)
- `projects/viz-nano-banana/2026-05-06_glp1-twitter-banner-v3.png` — Banner with logo + "Stop tracking. Start seeing." tagline in app colors
- Bio copy locked: "Your GLP-1 data in one place. See patterns, not scattered numbers. Built for people managing their journey — not another diet app."

### Decisions
- Spiral constellation logo preferred over geometric hexagon — feels more organic and data-pattern-forward
- Tagline-forward banner with logo mark included, content shifted right for Twitter profile pic overlap
- App redesign design system colors (teal #0F5F5A, coral #FF8A6A, amber #FFC66D) are the source of truth — not the old blue from assets.md

### Open threads
- Compare v3 spiral logo with existing logo in app-redesign folder tomorrow — user wants to potentially keep both
- Update assets.md color palette to match the app redesign design system (currently shows old blue #1C83E1)
- Profile assets generated but not yet uploaded to Twitter

### Deliverables
- `public/sw.js` — Service worker with push event, mark-taken, snooze, and notification click handlers
- `public/manifest.json` — PWA manifest for home screen install
- `src/lib/push.ts` — Client-side SW registration, subscribe/unsubscribe, permission utilities
- `src/lib/push-server.ts` — Server-side VAPID config, sendPushToUser, HMAC action token create/verify
- `src/components/push/sw-register.tsx` — Client component that registers SW on mount
- `src/components/push/notification-toggle.tsx` — Enable/disable reminders toggle on profile page
- `src/app/(app)/profile/push-actions.ts` — Server actions to save/remove push subscriptions
- `src/app/api/notifications/send/route.ts` — Cron endpoint that checks schedules and sends reminders
- `src/app/api/notifications/mark-taken/route.ts` — HMAC-verified endpoint to log medication from notification
- `src/app/api/notifications/snooze/route.ts` — Re-sends notification after 15-minute delay
- `prisma/schema.prisma` — Added PushSubscription model
- `src/proxy.ts` — Added `/api/notifications(.*)` to public routes

### Decisions
- Web Push (VAPID) over Firebase — no external dependency, server-side logic reusable when migrating to React Native
- HMAC-signed action tokens for SW auth — service worker fetch doesn't carry Clerk cookies
- Multi-dose support: count today's logs vs schedule.times.length instead of boolean taken check
- Time parser handles both "17:53" and "1753" formats to match existing data
- Cron set to */5 minutes via system crontab

### Open threads
- macOS Chrome doesn't show notification action buttons — Mark as Taken only works on Android/Windows
- Native iOS push requires React Native + Expo migration (swap web-push for expo-notifications)
- Cron only works while dev server runs on port 3010
