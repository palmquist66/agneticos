// This route group exists for future marketing pages (e.g., /about, /pricing).
// The root landing page lives at app/page.tsx.
// Exporting null component to satisfy TypeScript module requirement.
export default function Placeholder() {
  return null;
}
