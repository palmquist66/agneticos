---
// The live site has no per-page call-to-action band. A global "Ready to Visit?" +
// "Contact Us" section (see Footer.astro) now provides the CTA on every page, matching
// the live site. This component is intentionally a no-op so existing <CTA /> usages
// across pages render nothing rather than duplicating the global section.
interface Props {
  heading?: string;
  text?: string;
  variant?: 'green' | 'white';
}
---
