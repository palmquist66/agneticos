---
phase: quick
plan: 260505-lf2
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jamespalmquist/PycharmProjects/propread-web/backend/services/stats_service.py
autonomous: true
requirements: ["LF2-MLB-2026"]
must_haves:
  truths:
    - "MLB player stat lookups query L5/L10 game logs from the database using 2026 season data"
    - "MLB L5/L10 averages are calculated from real game-by-game data, not hardcoded to ESPN fallback"
    - "mlb_context dict is still returned alongside L5/L10 so prop_router.py gets game info, probable pitchers, opener detection"
    - "Pitcher season stats fetch tries 2026 first, falls back to 2025 if no data"
    - "All season labels in returned dicts say '2026 season' not '2025 season'"
  artifacts:
    - path: "/Users/jamespalmquist/PycharmProjects/propread-web/backend/services/stats_service.py"
      provides: "MLB L5/L10 from DB game logs + merged mlb_context"
      contains: "PlayerGameStats.game_id == Game.id"
  key_links:
    - from: "stats_service.py get_player_stats()"
      to: "Game table"
      via: "PlayerGameStats.game_id == Game.id join with Game.season == '2026'"
      pattern: "PlayerGameStats\\.game_id == Game\\.id"
    - from: "stats_service.py get_player_stats()"
      to: "_fetch_mlb_from_espn()"
      via: "Fallback when no DB game logs found, AND always called to get mlb_context"
      pattern: "_fetch_mlb_from_espn"
---

<objective>
Update stats_service.py so MLB props use real 2026 game log data from the database for L5/L10 averages instead of bypassing to 2025 ESPN season stats. The MLB season is well underway (6+ weeks in), so the early-season bypass is no longer needed.

Purpose: MLB prop analysis currently ignores all synced 2026 game data and falls back to stale 2025 season averages. This makes L5/L10 trends meaningless for MLB. The DB has real game-by-game data from sync_mlb.py that needs to be used.

Output: Updated stats_service.py with MLB-specific DB query path, merged mlb_context, and 2026 season references throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jamespalmquist/PycharmProjects/propread-web/backend/services/stats_service.py
@/Users/jamespalmquist/PycharmProjects/propread-web/backend/data/models.py
@/Users/jamespalmquist/PycharmProjects/propread-web/backend/sync_mlb.py

<interfaces>
<!-- Key schema facts the executor MUST know to avoid the wrong join -->

From data/models.py — PlayerGameStats:
- game_id = Column(Integer, ForeignKey('games.id'))  # This is an INTEGER FK to Game.id
- For MLB records, game_id stores Game.id (auto-increment), NOT the nba_game_id string
- MLB stat columns (batting): hits, home_runs, total_bases, rbis, runs_scored, stolen_bases, walks, singles, doubles, at_bats, batter_strikeouts
- MLB stat columns (pitching): innings_pitched, earned_runs, hits_allowed, walks_allowed, pitcher_strikeouts, outs_recorded, pitches_thrown, batters_faced

From data/models.py — Game:
- id = Column(Integer, primary_key=True)  # Auto-increment
- nba_game_id = Column(String(20))  # For MLB: "mlb_{gamePk}" string
- season = Column(String(10))  # For MLB: "2026" (not "2025-26" like NBA)

From sync_mlb.py — How MLB data is stored:
- Players: nba_player_id = "mlb_{mlb_id}"
- Games: nba_game_id = "mlb_{gamePk}", season = "2026"
- PlayerGameStats.game_id = Game.id (integer FK, set via db.flush() after Game insert)

CRITICAL JOIN DIFFERENCE:
- NBA/NHL join: cast(PlayerGameStats.game_id, String) == Game.nba_game_id  (string comparison)
- MLB join: PlayerGameStats.game_id == Game.id  (integer FK, standard join)
- The NBA/NHL join WILL NOT WORK for MLB because MLB game_id stores the integer Game.id, not the string nba_game_id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace MLB early-season bypass with DB game log query + merge mlb_context</name>
  <files>/Users/jamespalmquist/PycharmProjects/propread-web/backend/services/stats_service.py</files>
  <action>
Make these changes to stats_service.py:

**Change 1 — Replace lines 98-106 (the MLB early-season bypass) with MLB-specific DB game log query:**

Remove the block:
```python
# MLB — early season: skip L5/L10 game logs, use 2025 season stats only
# TODO: Re-enable DB game log L5/L10 once ~15 games into the 2026 season
if sport == "mlb":
    print(f"⚾ MLB early season mode — skipping L5/L10, using 2025 stats for {player}")
    espn_stats = await _fetch_mlb_from_espn(player, stat, db)
    db.close()
    if espn_stats:
        return espn_stats
    return _no_stats_available()
```

Replace with MLB-specific game log query that:
1. Maps the stat name to the correct MLB DB column using the existing `MLB_STAT_MAP` dict (defined on lines 80-96). Apply the mapping: `mlb_col = MLB_STAT_MAP.get(stat_column, stat_column)` — this translates user-facing stat names (e.g., "runs") to DB column names (e.g., "runs_scored").
2. Queries PlayerGameStats joined with Game using the INTEGER FK join: `PlayerGameStats.game_id == Game.id` (NOT the cast/string join used for NBA/NHL).
3. Filters by `PlayerGameStats.player_id == player_record.id` AND `Game.season == "2026"`.
4. Orders by `desc(Game.game_date)` and limits to 10.
5. If DB game logs are found, extracts stat values using `getattr(game_stat, mlb_col, None)` and calculates L5/L10 averages exactly like the NBA/NHL path does (lines 178-219).
6. ALSO calls `await _fetch_mlb_from_espn(player, stat, db)` to get the `mlb_context` dict (game info, probable pitchers, opener detection, season stats).
7. Merges the DB-computed L5/L10 into the ESPN result: overwrites `l5_average`, `l10_average`, `l5_games` with the DB values, keeps `mlb_context` from ESPN.
8. If NO DB game logs found, falls back to the ESPN-only path (same as current behavior, but now explicitly a fallback rather than the only path).
9. Close DB session and return. Make sure `db.close()` is called on all paths.

The merged return dict should look like:
```python
{
    "l5_average": <from DB>,
    "l10_average": <from DB>,
    "l5_games": <from DB>,
    "opponent": None,
    "opp_rank_label": None,
    "injury_flag": None,
    "mlb_context": <from ESPN>,  # game_info, probable_pitchers, opener, season stats
}
```

Print a log line: `f"⚾ MLB L5/L10 from DB: L5={l5_avg}, L10={l10_avg}, games={len(stat_values)}"`

**Change 2 — Update `_get_pitcher_season_stats()` (lines 522-547) to try 2026 first:**

Change the function to:
1. First try `season: "2026"` in the MLB API call.
2. If the response has stats (splits is non-empty), return those.
3. If 2026 returns no splits, retry with `season: "2025"` as fallback.
4. Update the function docstring to say "Fetch 2026 pitching stats (fallback to 2025)".

**Change 3 — Update `_fetch_mlb_from_espn()` season references:**

1. Line 682 comment: change "Step 2: Fetch 2025 season stats" to "Step 2: Fetch 2026 season stats (fallback to 2025)"
2. Lines 693-695: Change `"season": "2025"` to `"season": "2026"` in the stats API params.
3. Add fallback: if the 2026 stats response has empty splits, retry with `"season": "2025"`.
4. Lines 758, 778, 800, 815: Change `"season_label": "2025 season"` to `"season_label": "2026 season"`.
5. Line 778: Change `"data_note": "2025 season stats — 2026 data updating as season progresses"` to `"data_note": "2026 season stats"`.
6. Line 815: Same data_note update.
7. Lines 655-662 (opener detection stats check): Change `"season": "2025"` to `"season": "2026"` (with same fallback pattern — try 2026, if no data try 2025).
8. Line 869 in `_mlb_knowledge_only_fallback()`: Update `"season_label"` to `"2026 season"` and `"data_note"` to reference 2026.
  </action>
  <verify>
    <automated>cd /Users/jamespalmquist/PycharmProjects/propread-web && python -c "
import ast
with open('backend/services/stats_service.py') as f:
    source = f.read()
# Verify early-season bypass is removed
assert 'early season mode' not in source, 'Early season bypass still present'
assert 'Re-enable DB game log' not in source, 'TODO comment still present'
# Verify correct MLB join is used
assert 'PlayerGameStats.game_id == Game.id' in source, 'MLB integer FK join not found'
# Verify 2026 season filter
assert \"Game.season == \\\"2026\\\"\" in source or \"Game.season == '2026'\" in source, '2026 season filter not found'
# Verify season labels updated
assert '2025 season' not in source.split('def _fetch_mlb_from_espn')[1] if '_fetch_mlb_from_espn' in source else True, '2025 season label still in ESPN func'
# Verify file is valid Python
ast.parse(source)
print('All checks passed')
"
    </automated>
  </verify>
  <done>
    - The MLB early-season bypass (lines 98-106) is removed
    - MLB L5/L10 game logs are queried from DB using correct integer FK join (PlayerGameStats.game_id == Game.id) with Game.season == "2026"
    - mlb_context from _fetch_mlb_from_espn() is merged with DB-computed L5/L10 averages
    - Falls back to ESPN-only when no DB game logs exist
    - _get_pitcher_season_stats() tries 2026 first, falls back to 2025
    - _fetch_mlb_from_espn() uses 2026 season with 2025 fallback
    - All season labels say "2026 season"
    - File parses as valid Python with no syntax errors
  </done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('/Users/jamespalmquist/PycharmProjects/propread-web/backend/services/stats_service.py').read()); print('Valid Python')"` — confirms no syntax errors
2. Grep for `early season mode` returns zero matches — bypass removed
3. Grep for `PlayerGameStats.game_id == Game.id` returns at least 1 match — correct MLB join
4. Grep for `"2025 season"` in the ESPN function returns zero matches — labels updated
5. Grep for `"season": "2026"` in pitcher stats and ESPN function confirms 2026 is primary
</verification>

<success_criteria>
- MLB player stat lookups compute L5/L10 from real 2026 DB game logs
- mlb_context (game info, pitchers, opener detection) still flows through to prop_router.py
- Pitcher season stats use 2026 data when available
- No references to "2025 season" remain in output labels
- stats_service.py is valid Python
</success_criteria>

<output>
After completion, create `.planning/quick/260505-lf2-update-mlb-stats-service-py-to-use-2026-/260505-lf2-SUMMARY.md`
</output>
